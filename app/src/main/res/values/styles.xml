<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--图标样式-->
    <declare-styleable name="MyGraphStyle">
        <attr name="graphTitle" format="string"></attr>
        <attr name="xAxisName" format="string"></attr>
        <attr name="yAxisName" format="string"></attr>
        <attr name="axisTextSize" format="dimension|integer"></attr>
        <attr name="axisTextColor" format="color|integer"></attr>

        <attr name="showOrigin" format="boolean"></attr>
        <attr name="showXAxis" format="boolean"></attr>
        <attr name="showYAxis" format="boolean"></attr>
        <attr name="axisColor" format="integer|color|reference"></attr>
    </declare-styleable>
    
</resources>
